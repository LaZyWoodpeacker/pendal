export interface IOnecCreateResultData {
  messageId: string;
  copyMessageId: string;
  boxId: string;
  updNumber: string;
}

export interface IOnecCreateResultReportData {
  messageId: string;
  boxId: string;
  updNumber: string;
  reportNumber: string;
}
